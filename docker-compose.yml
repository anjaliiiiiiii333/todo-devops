version: "3.9"

services:
  web:
    build: .
    image: todo-app:local
    container_name: todo-app
    ports:
      - "5000:5000"
    volumes:
      - todo-data:/app/data
    restart: unless-stopped

volumes:
  todo-data:
